export default interface IProduct {
    id: number;
    code: string;
    price: number;
    stock: number;
    category: string;
    image: string;
    // quantity: number;
    // thumbnail: string;
}